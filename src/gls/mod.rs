mod lsrange;
mod lsinit;
mod lssort;
mod lssplit;
mod lsnew;
// const SHORT: f64 = 0.381966011250105; // golden section fraction = (3-sqrt(5))/2
//
//
// pub fn gls(func: &str, data: Vec<f64>, xl: f64, xu: f64, x: f64, p: Vec<f64>,
//            alist: Vec<f64>, flist: Vec<f64>, nloc: f64, small: f64, smax: f64) {
//     let sinit = data.len();
// }
